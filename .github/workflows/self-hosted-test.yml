name: Self-Hosted Runner Smoke Test

on:
  push:
    paths:
      - '.github/workflows/self-hosted-test.yml'

jobs:
  smoke:
    runs-on: [self-hosted, macOS, X64]
    steps:
      - name: Runner check-in
        run: echo "✅ Faqihahs-MacBook-Pro is live!"
