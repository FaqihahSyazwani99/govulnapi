name: 🔍 BOLA Security Scan - Enterprise Grade

on:
  push:
    branches: [ main, develop ]
  pull_request:

jobs:
  bola-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📅 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🖍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 💡 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests tenacity jq

      - name: 🔢 Identify target files
        #continue-on-error: true  # Prevent job from failing here
        run: |
          SCAN_DIR="."
          echo "📁 Scanning directory: $SCAN_DIR"
    
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            BASE_REF="origin/${{ github.base_ref }}"
          else
            BASE_REF="HEAD^"
          fi
    
          echo "🔍 Comparing changes from $BASE_REF to HEAD"
    
          CHANGED_FILES=$(git diff --name-only --diff-filter=AM $BASE_REF HEAD -- "$SCAN_DIR" | grep -E '\.(go|js|py|ts|java)$' || true)
          echo "🧾 CHANGED_FILES: $CHANGED_FILES"
    
          ALL_FILES=$(find "$SCAN_DIR" -type f \( -name "*.go" -o -name "*.js" -o -name "*.py" -o -name "*.ts" -o -name "*.java" \))
          echo "📦 ALL_FILES:"
          echo "$ALL_FILES"

          if [ -z "$CHANGED_FILES" ]; then
            FILES="$ALL_FILES"
            echo "ℹ️ No changed files, scanning ALL files."
          else
            FILES="$CHANGED_FILES"
            echo "ℹ️ Scanning only CHANGED files."
          fi

          TARGET_FILES=$(printf '%s\n' $FILES | jq -R -s -c 'split("\n") | map(select(. != ""))')
          echo "🎯 TARGET_FILES: $TARGET_FILES"
          echo "TARGET_FILES=$TARGET_FILES" >> $GITHUB_ENV
          
      - name: 🚡 Run Enhanced BOLA Scanner
        env:
          TARGET_FILES: ${{ env.TARGET_FILES }}
          LANGCHAIN_API_KEY: ${{ secrets.LANGCHAIN_API_KEY }}
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
          OLLAMA_ENDPOINT: ${{ secrets.OLLAMA_ENDPOINT }}
        run: |
          python .github/workflows/bola_scanner.py

      - name: 📅 Upload Scan Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bola-scan-results
          path: |
            bola-results.json
            bola-results.sarif

      - name: 📊 Upload SARIF Report
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bola-results.sarif

      - name: ❌ Fail on vulnerabilities
        if: failure()
        run: |
          echo "::error::BOLA vulnerabilities detected! Review scan artifacts"
          exit 1
